import 'package:flutter/material.dart';

class AppColors {
  // Paleta oficial VoDiBus!
  static const Color azulEscuro = Color(0xFF1E3A5F); // Estrutura, texto
  static const Color amarelo = Color(0xFFFFC107); // Destaque — "Di"
  static const Color azulMedio = Color(0xFF007AFF); // Ação — botões, "!"
  static const Color verdeAzulado = Color(0xFFE8F5F0); // Fundo splash
  static const Color verde = Color(0xFF34C759); // Confirmações

  // Neutros
  static const Color branco = Color(0xFFFFFFFF);
  static const Color cinzaFundo = Color(0xFFF5F5F5);
  static const Color cinzaTexto = Color(0xFF666666);
  static const Color cinzaClaro = Color(0xFFCCCCCC);
  static const Color textoPrincipal = Color(0xFF1A1A1A);
}
